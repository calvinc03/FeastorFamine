#pragma once

#include "common.hpp"
#include "entt.hpp"

// Projectile from
struct TalkyBoi
{
    // Creates all the associated render resources and default transform
    static entt::entity createTalkyBoiEntt();
};

